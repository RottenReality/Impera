{
  "name": "@impera/shared",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx --ignore-path .gitignore"
  },
  "lint-staged": {
  "*.{ts,tsx,js,jsx}": [
    "pnpm lint --fix",
    "pnpm format"
  ],
  "*.{json,md}": [
    "pnpm format"
  ]
}
}